class Pizza {
  final String pizzaType;
  final String name;
  final String address;
  final int size;
  final bool extraCheese;

  Pizza({this.pizzaType, this.name, this.address, this.size, this.extraCheese});
}
